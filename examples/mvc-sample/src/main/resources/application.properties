# The report endpoint ships disabled. This is a local demo bound to 127.0.0.1,
# so it is turned on here; do not do this in a deployed application without
# putting your own authentication in front of the path.
reqover.report.endpoint.enabled=true
reqover.report.endpoint.path=/reqover/report
